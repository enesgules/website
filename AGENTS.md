# Deployment

Pushing the requested changes to GitHub is enough to deploy this site. Do not run `pnpm deploy`, `wrangler deploy`, or any other manual Cloudflare deployment command unless the user explicitly asks for it.
